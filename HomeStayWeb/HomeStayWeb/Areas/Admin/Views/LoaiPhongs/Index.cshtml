@model IEnumerable<HomeStayWeb.Models.LoaiPhong>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenLP)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenLP)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDLP }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDLP }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDLP })
        </td>
    </tr>
}

</table>
